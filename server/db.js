/**
 * Database module — currently unused (switched to Odoo JSON-RPC API).
 * Keeping file for potential future direct DB access.
 * See server/odooClient.js for the active data source.
 */

module.exports = { query: null, queryFromFile: null };